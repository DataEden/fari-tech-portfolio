
# 🐾 PetPals AI Integration Notebook – Backend + Insights

This notebook connects Supabase backend data to your data science logic to:
- Parse raw logs from the API
- Run AI-like analysis
- Output summary insights

---

## 📦 Setup

```python
import os
import json
from dotenv import load_dotenv
from raw_log_data import parse_raw_pet_data
from pet_health_analysis import analyze_pet_health_and_activity

# Load .env file
load_dotenv()

SUPABASE_URL = os.getenv("SUPABASE_URL")
SUPABASE_KEY = os.getenv("SUPABASE_KEY")
```

---

## 🔌 Fetch & Preview Supabase Data (Replace with actual query)

```python
# Simulate raw data (replace with actual Supabase fetch later)
with open("mock_supabase_rawdata.json") as f:
    raw_data = json.load(f)

# Print sample
print(json.dumps(raw_data[0], indent=2))
```

---

## 🔍 Parse Raw Log Data (Meals, Walks, Weight)

```python
summary = parse_raw_pet_data(raw_data)
print("=== Raw Summary ===")
print(json.dumps(summary, indent=2))
```

---

## 🧠 Analyze One Pet’s Health + Routine

```python
# Simulated single pet logs from your Supabase
single_pet_logs = {
    "pet_name": summary[0]["pet_name"],
    "logs": {
        "health": [
            {"date": "2025-06-26", "mood": "low", "symptoms": "itchy"},
            {"date": "2025-06-27", "mood": "low", "symptoms": "none"}
        ],
        "tasks": [
            {"date": "2025-06-26", "feeding_8am": 0, "feeding_6pm": 1, "walk_morning": 0, "walk_evening": 1},
            {"date": "2025-06-27", "feeding_8am": 1, "feeding_6pm": 1, "walk_morning": 1, "walk_evening": 0}
        ]
    }
}

insight = analyze_pet_health_and_activity(single_pet_logs)
print("=== AI-Like Insight ===")
print(json.dumps(insight, indent=2))
```

---

## ✅ Next:
- Replace mock file with real Supabase fetch
- Pass real data to parser + analysis
- Export results to text or use in Figma UI mock
